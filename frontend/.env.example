# Copy this file to frontend/.env for local development.
# Base URL of the VidPoint backend (no trailing slash).
# Local development:
VITE_API_URL=http://localhost:8000
# Production (Vercel): set this to your Render backend URL, e.g.
# VITE_API_URL=https://vidpoint-backend.onrender.com
